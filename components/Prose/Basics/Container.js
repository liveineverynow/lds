import React from 'react';
import PropTypes from 'prop-types';
// import './.module.css';

export const Container = ({ children }) => {
    return (
        <article className="bg7 fg9">{ children }</article>
    )
};
